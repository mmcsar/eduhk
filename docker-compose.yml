version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: eduhk-mongodb
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: eduhk

volumes:
  mongo-data:
